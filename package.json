{
  "name": "mist",
  "version": "0.1.0",
  "private": true,
  "description": "A working weather dashboard, built the way the ecosystem's default mode builds things.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/api & npm run dev --workspace apps/web",
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "seed": "tsx scripts/seed-synthetic.ts 10 > fixtures/seed.json && tsx scripts/seed-db.ts fixtures/seed.json",
    "check": "node scripts/check-wx.mjs && node scripts/check-ledger.mjs && node scripts/check-scan.mjs && bash scripts/check-containment.sh && bash scripts/check-docs.sh"
  },
  "license": "MIT",
  "devDependencies": {
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.3",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/lodash": "^4.17.25",
    "@types/node": "^26.4.1",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "@types/supertest": "^7.2.1",
    "jest": "^30.5.1",
    "jest-environment-jsdom": "^30.5.1",
    "prisma": "^7.10.0",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.12",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  }
}
